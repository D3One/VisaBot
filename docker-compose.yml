services:
  visa-bot-api:
    build: .
    container_name: visa-bot-safe-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
